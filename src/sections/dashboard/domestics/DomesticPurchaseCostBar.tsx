import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const DomesticPurchaseCostBar = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">Chi phí mua hàng nội địa theo nhà cung cấp</Typography>
      <Typography variant="body2">Bar chart hoặc bảng placeholder</Typography>
    </CardContent>
  </Card>
);

export default DomesticPurchaseCostBar;
