import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const TodoTaskList = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">Todo Task List (QC-Nội địa)</Typography>
      <Typography variant="body2">Danh sách công việc cần xử lý (placeholder)</Typography>
    </CardContent>
  </Card>
);

export default TodoTaskList;
