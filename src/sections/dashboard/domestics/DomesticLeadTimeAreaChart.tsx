import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const DomesticLeadTimeAreaChart = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">Biểu đồ thời gian đặt hàng → nhận hàng (Lead Time)</Typography>
      <Typography variant="body2">Area chart placeholder</Typography>
    </CardContent>
  </Card>
);

export default DomesticLeadTimeAreaChart;
