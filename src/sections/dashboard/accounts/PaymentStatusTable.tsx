import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const PaymentStatusTable = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">Danh sách công việc (Kế toán)</Typography>
      <Typography variant="body2">Table trạng thái thanh toán/đối chiếu công nợ (placeholder)</Typography>
    </CardContent>
  </Card>
);

export default PaymentStatusTable;
