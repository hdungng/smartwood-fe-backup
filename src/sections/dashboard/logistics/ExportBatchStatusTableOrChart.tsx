import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const ExportBatchStatusTableOrChart = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">Danh sách lô hàng xuất khẩu theo trạng thái</Typography>
      <Typography variant="body2">Table hoặc Status Column Chart placeholder</Typography>
    </CardContent>
  </Card>
);

export default ExportBatchStatusTableOrChart;
